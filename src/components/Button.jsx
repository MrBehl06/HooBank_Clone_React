import React from 'react'
import styles from '../styles'

const button = () => (
    <div>
      <button type='button' className={`py-4 px-6 mt-3 bg-blue-gradient font-poppins font-medium text-[18px] text-primary outline-none ${styles}`}>
      Get Started
      </button>
    </div>
  )

export default button
